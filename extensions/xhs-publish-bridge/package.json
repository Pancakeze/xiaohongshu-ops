{
  "name": "xhs-publish-bridge",
  "version": "0.1.9",
  "private": true,
  "description": "Chrome MV3 extension: ops console → Xiaohongshu creator page fill-in",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "watch": "node esbuild.config.mjs --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0"
  }
}
